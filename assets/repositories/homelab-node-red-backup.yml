---
name: homelab-node-red-backup
description: "Homelab: Python application for backing up and restoring Node-RED via its API"
visibility: public
protected: true
topics:
  - node-red
  - homelab

rulesets:
  branch:
    enabled: true
    requiredChecks:
      - Dependency Review
      - Check the Codebase (3.12, 1.7.1)
      - Build (3.12)

accessPermissions:
  vault:
    enabled: false
