---
name: postgresql-pgvecto-container
description: "A container integrating pgvecto.rs into CloudNativePG's PostgreSQL container"
visibility: public
protected: true
topics:
  - postgresql
  - pgvecto-rs
  - cloudnative-pg
  - container

rulesets:
  branch:
    enabled: true
    requiredChecks:
      - Dependency Review
      - Lint Dockerfile
      - Build Container Image

accessPermissions:
  vault:
    enabled: false
