---
name: homelab-kubernetes-home-infrastructure
description: "Homelab: Infrastructure for the Kubernetes home-cluster"
visibility: public
protected: true
topics:
  - kubernetes
  - infrastructure
  - homelab

rulesets:
  branch:
    enabled: true
    requiredChecks:
      - Dependency Review
      - YAML Lint
      - Lint (1.26.x)
      - Unit Tests (1.26.x)
      - Pulumi Preview (1.26.x, vie)
      # - Pulumi Preview (1.26.x, muc)

accessPermissions:
  vault:
    enabled: true
    additionalMounts:
      - path: kubernetes-home-cluster
        create: true
        permissions:
          - create
          - read
          - update
          - delete
          - list
      - path: kubernetes-home-cluster-muc
        create: true
        permissions:
          - create
          - read
          - update
          - delete
          - list
  tailscale: true
  google:
    region: europe-west4
    project: tuxnet-385112
    linkedProjects:
      muehlbachler-dns:
        accessLevel: default
  scaleway:
    project: tuxnet
    linkedProjects:
      dns-management:
        iamPermissions:
          - DomainsDNSFullAccess
