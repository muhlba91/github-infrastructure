---
name: muehlbachler-hashicorp-vault-infrastructure
description: "Personal Core Infrastructure"
visibility: public
protected: true
topics:
  - services
  - infrastructure
  - vault

rulesets:
  branch:
    enabled: true
    requiredChecks:
      - Dependency Review
      - ESLint (24)
      - Build IaC code (24)
      - Pulumi Preview (24)

# disable all permissions to avoid the chicken-egg problem
# we only manage the repository settings here
# everything else is managed manually
accessPermissions:
  vault:
    enabled: false
  tailscale: false
